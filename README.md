# Cinema Suggestion Application

- To start the application, run: `npm install | npm start`

- Create a .env file and complete it with the correct data (Look at the .env.example file for the structure)
